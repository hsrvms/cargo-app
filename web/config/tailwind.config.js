tailwind.config = {
  darkMode: "class", // important! must be 'class' for toggle to work
  theme: {
    extend: {
      colors: {
        primary: "#3B82F6",
        secondary: "#6B7280",
      },
    },
  },
};
